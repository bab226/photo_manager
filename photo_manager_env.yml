name: photo_manager_env
channels:
  - conda-forge
  - defaults  # Fallback to default channel as needed
dependencies:
  - python=3.9.18
  - dlib
  - mtcnn
  - opencv
  - face_recognition
  - pillow
  - numpy
  - scikit-learn
  - matplotlib
  - piexif
  - pytest